# Edge Functions

Milestone 12A intentionally defines no Edge Function. Trusted workflow writes,
document numbering, and server-side stock posting are deferred to Milestone 12B.
This directory is kept so future server code has an explicit, version-controlled
home and secrets remain outside source control.
